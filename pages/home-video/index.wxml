<!--pages/home-video/index.wxml-->
<view class="video">
	<view class="item" wx:for="{{topMvs}}" wx:key="id">
		<video-item-v1
		 item="{{item}}"
		 bindtap="handleVideoItemClick"
		 data-item="{{item}}"
		 vslide-gesture="{{true}}"
		/>
	</view>
</view>

